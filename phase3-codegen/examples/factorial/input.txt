/* Recursive factorial: exercises the runtime stack, frame pointer,
   activation records and return values. Prints 120. */
int fact(int n) {
    int f;
    if (n == 1) f = 1;
    else f = n * fact(n - 1);
    return f;
}

void main(void) {
    int i;
    i = fact(5);
    output(i);
}
